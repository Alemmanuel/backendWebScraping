{
  "name": "backend-webscraping",
  "version": "1.0.0",
  "description": "Backend for web scraping with Puppeteer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && apt-get update && apt-get install -y chromium"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer": "^24.4.0"
  },
  "engines": {
    "node": "18.x"
  }
}