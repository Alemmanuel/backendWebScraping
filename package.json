{
  "name": "backend-webscraping",
  "version": "1.0.0",
  "description": "Backend for web scraping with Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "^1.42.1"
  },
  "engines": {
    "node": "18.x"
  }
}